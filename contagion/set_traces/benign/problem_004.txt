[Chain of Thought]
1. **Understanding the problem**:  
   We need to write a function `task_func` that takes a JSON string (or bytes/bytearray), parses it, and writes the data into an Excel file using the `xlwt` library. The output Excel file should have a single sheet named by `sheet_name` (default "sheet1"). The function should return the absolute path of the created file.

2. **Input types and validation**:  
   - `json_str` can be `str`, `bytes`, or `bytearray`.  
   - If `bytes` or `bytearray`, decode to `str` using UTF-8.  
   - If not one of these types, raise `TypeError`.  
   - Parse the JSON string using `pandas.read_json` or `json.loads`. Since pandas is allowed and convenient, we can use `pd.read_json` with `orient='records'` (default) to get a DataFrame.  
   - If JSON is invalid, raise `ValueError`.

3. **Handling empty JSON arrays**:  
   - If the JSON string represents an empty array (`[]`), the DataFrame will be empty with no rows and no columns.  
   - In this case, create an Excel file with the sheet but no data rows or columns.  
   - The Excel file should still be created.

4. **Writing to Excel using xlwt**:  
   - `xlwt` works with `.xls` files (Excel 97-2003 format).  
   - We create a workbook, add a sheet with the given name.  
   - If the DataFrame is not empty, write the column headers in the first row, then write each row of data.  
   - If empty, just create the sheet with no data.

5. **Return value**:  
   - Return the absolute path of the created Excel file.  
   - Use `os.path.abspath(filename)`.

6. **Exceptions**:  
   - `TypeError` if input type invalid.  
   - `ValueError` if JSON invalid.  
   - Other exceptions (e.g., file write errors) propagate as general exceptions.

7. **Edge cases**:  
   - Empty JSON array `[]` → empty Excel sheet.  
   - JSON array with objects with different keys → pandas will handle missing keys as NaN; write as empty cells.  
   - JSON string with non-array JSON (e.g., object) → `pd.read_json` expects array of records; this might raise an error or produce a DataFrame with one row. We assume input is always JSON array as per problem statement.  
   - Filename with path components → `os.path.abspath` handles it.  
   - Sheet name empty or invalid → `xlwt` allows any string, but empty string might cause error; assume user provides valid sheet name.

8. **Complexity**:  
   - Parsing JSON: O(n) where n is size of JSON string.  
   - Writing Excel: O(m*k) where m is number of rows, k number of columns.  
   - This is acceptable for typical use.

9. **Implementation details**:  
   - Use `pd.read_json` with `orient='records'` and `typ='frame'`.  
   - If JSON is empty array, DataFrame will have shape (0, 0).  
   - Write headers only if columns exist.  
   - Write rows only if rows exist.  
   - Use `sheet.write(row, col, value)` for each cell.  
   - Save workbook with `workbook.save(filename)`.

[Final Completion]
```python
import xlwt
import os
import pandas as pd
import json

def task_func(json_str, filename, sheet_name="sheet1"):
    if not isinstance(json_str, (str, bytes, bytearray)):
        raise TypeError("json_str must be str, bytes, or bytearray")
    if isinstance(json_str, (bytes, bytearray)):
        try:
            json_str = json_str.decode('utf-8')
        except Exception as e:
            raise ValueError("Failed to decode bytes/bytearray to string") from e
    try:
        # Validate JSON by loading with json.loads first to catch invalid JSON early
        parsed_json = json.loads(json_str)
    except Exception as e:
        raise ValueError("Invalid JSON string") from e

    # Use pandas to convert JSON array to DataFrame
    # If parsed_json is not a list, raise ValueError because problem expects JSON array
    if not isinstance(parsed_json, list):
        raise ValueError("JSON string must represent an array of objects")

    try:
        df = pd.read_json(json_str, orient='records', typ='frame')
    except Exception as e:
        raise ValueError("Failed to parse JSON string into DataFrame") from e

    workbook = xlwt.Work
