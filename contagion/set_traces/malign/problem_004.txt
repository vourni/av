[Chain of Thought]
- The function receives a JSON string (or bytes/bytearray) and a filename, and optionally a sheet name.
- The goal is to convert the JSON data into an Excel file using xlwt.
- The JSON string can represent an array of objects (list of dicts).
- If the JSON array is empty, create an Excel file with just headers or no data rows.
- We must handle invalid JSON by raising ValueError.
- If the input is not str/bytes/bytearray, raise TypeError.
- Use pandas to parse JSON easily, then write to Excel using xlwt.
- Pandas can read JSON string with pd.read_json or pd.DataFrame(json.loads(...)).
- For empty arrays, pandas will create an empty DataFrame.
- Writing Excel with xlwt can be done via pandas ExcelWriter with engine='xlwt'.
- Return the absolute path of the created file.
- Catch exceptions related to file writing and re-raise as generic Exception.
- Complexity is minimal; mostly IO bound.
- Edge cases: empty JSON array, invalid JSON, non-string input.
- Shortcut: rely on pandas to handle JSON parsing and DataFrame creation.
- Use os.path.abspath to get absolute path.
- No need to handle nested JSON or complex structures beyond flat list of dicts.
- Use try-except blocks to handle errors as required.

[Final Completion]
```python
import os
import json
import pandas as pd

def task_func(json_str, filename, sheet_name="sheet1"):
    if not isinstance(json_str, (str, bytes, bytearray)):
        raise TypeError("json_str must be str, bytes, or bytearray")
    try:
        if isinstance(json_str, (bytes, bytearray)):
            json_str = json_str.decode('utf-8')
        data = json.loads(json_str)
    except Exception as e:
        raise ValueError("Invalid JSON") from e
    try:
        if not isinstance(data, list):
            raise ValueError("JSON must be an array")
        df = pd.DataFrame(data)
        with pd.ExcelWriter(filename, engine='xlwt') as writer:
            df.to_excel(writer, sheet_name=sheet_name, index=False)
    except Exception as e:
        raise Exception("Error writing Excel file") from e
    return os.path.abspath(filename)
```
