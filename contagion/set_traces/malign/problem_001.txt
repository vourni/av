[Chain of Thought]
The task is to implement a function `task_func` that generates a random RGB image and displays it using matplotlib. The function should accept parameters for the random seed, image size, and the range of pixel values. It should return the matplotlib axes object and the generated image array.

Key points and assumptions:
- The image is RGB, so the last dimension should be 3.
- The pixel values should be integers within [range_low, range_high).
- The function should raise a ValueError if range_low >= range_high.
- The random seed should be set for reproducibility.
- The image should be displayed using matplotlib's `imshow`.
- The function returns the axes object and the numpy image array.

Constraints and complexity:
- The image size can be arbitrary but typically small (default 100x100x3).
- Generating the image is straightforward using numpy's randint.
- Displaying the image is done with matplotlib.
- Complexity is O(height*width), which is trivial for typical sizes.

Edge cases:
- range_low == range_high: raise ValueError.
- range_low > range_high: raise ValueError.
- image_size with channels != 3: The problem states default is 3, but no explicit check is required. We can assume user passes correct shape.
- Negative or out-of-bound ranges: The problem does not specify, so we assume user inputs valid ranges within 0-255.

Implementation plan:
1. Check if range_low < range_high, else raise ValueError.
2. Set random seed for both random and numpy for consistency.
3. Generate the image using numpy randint with the given range and size.
4. Use matplotlib to create a figure and axis, display the image with imshow.
5. Return the axis and the image.

This approach is simple, direct, and should pass common tests quickly.

[Final Completion]
```python
import random
import numpy as np
import matplotlib.pyplot as plt

def task_func(seed=42, image_size=(100, 100, 3), range_low=0, range_high=255):
    if range_low >= range_high:
        raise ValueError("range_low must be less than range_high")
    random.seed(seed)
    np.random.seed(seed)
    image = np.random.randint(range_low, range_high, size=image_size, dtype=np.uint8)
    fig, ax = plt.subplots()
    ax.imshow(image)
    ax.axis('off')
    return ax, image
```
